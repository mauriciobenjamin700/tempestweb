// transport-wasm.js — Mode A transport over pyodide.ffi.  PHASE A1.
// Bridges the in-browser Python reconciler to the DOM renderer in-process.
// Implements the Transport interface from transport.js.
export function createWasmTransport(/* pyodide handles */) {
  throw new Error("A1: createWasmTransport not implemented");
}
