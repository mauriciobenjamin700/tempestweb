// transport-ws.js — Mode B transport over WebSocket.  PHASE B1.
// Carries patch batches (server->client) and events (client->server) as JSON.
// Implements the Transport interface from transport.js.
export function createWebSocketTransport(/* url */) {
  throw new Error("B1: createWebSocketTransport not implemented");
}
