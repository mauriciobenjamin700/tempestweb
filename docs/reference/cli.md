# `tempestweb.cli`

A implementação do comando `tempestweb` — `new`, `build`, `dev`, `deploy`, `gen`, `sync`. Documentado para quem quer chamar um comando de dentro do Python ou estender a CLI; para usá-la no terminal, a página de uso é o lugar certo.

Guia com exemplos: [Usando a CLI](../tutorial/cli.md).

::: tempestweb.cli
