# `tempestweb.core`

Cross-cutting constants shared across the framework. Not to be confused with the [`tempest-core`](https://pypi.org/project/tempest-core/) package, the published dependency where the IR, reconciler, state, style and widgets live.

Guide with examples: [Architecture](../architecture.md).

::: tempestweb.core
