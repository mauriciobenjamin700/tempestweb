# `tempestweb.export`

Geração de CSV e XLSX em Python puro, para o [`native.file.save`](../advanced/native-reference.md) entregar. Modos A e B (o Modo C recusa o import no build). Não instala nada — `zipfile` e `xml.etree` da stdlib bastam para o XLSX.

Guia com exemplos: [Exportar CSV e XLSX](../advanced/export.md).

::: tempestweb.export
