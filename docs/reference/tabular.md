# `tempestweb.tabular`

Inferência sklearn→ONNX no browser, irmã do [`vision`](vision.md). O **manifesto** — quais features o modelo espera e em que ordem — é o que impede a predição silenciosamente errada. Modos A e B (o Modo C recusa o import no build). Treinar e exportar são passo de build em venv descartável, não dependência.

Guia com exemplos: [Inferência tabular no browser](../advanced/tabular.md).

::: tempestweb.tabular
